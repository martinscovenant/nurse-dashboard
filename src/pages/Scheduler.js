import React from "react";

const Scheduler = () => {
    return (
        <div>
            <h2>Scheduler</h2>
            <p>Manage appointments here</p>
        </div>
    );
};

export default Scheduler;